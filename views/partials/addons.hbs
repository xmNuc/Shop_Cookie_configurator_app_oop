<h3>Selected parts of cookie</h3>
<ul>
  <li>Base: {{cookie.base}} - {{pricify (findPrice allBases cookie.base)}} $</li>
  {{#each cookie.addons}}
    <li>

      {{#unless ../noDeleteBtn}}

      <a href='/configurator/delete-addon/{{this}}'>
        <button>Delete</button></a>
{{/unless}}

      Addon:
      {{this}}
      -
      {{pricify (findPrice @root.allAddons this)}}
      $

    </li>
  {{/each}}
</ul>
<p>
  Sum
  {{pricify sum}}
  $
</p>